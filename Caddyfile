{
	# Admin API on :2019 (internal only — not exposed to the internet)
	admin :2019
}

:80 {
	# API routes
	handle /api/* {
		reverse_proxy api:8080
	}

	# Deployment routes are registered dynamically via the Caddy Admin API.
	# Each subdomain gets its own route prepended above this block.
}
